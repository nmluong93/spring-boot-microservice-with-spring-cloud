Configuration management for microservices with config-server of spring cloud

- configserver
  - this is Spring Cloud Config server for entire microservice applications

- docker-compose:
  - This is concentrating docker-compose file for each environment (spring boot profile) deployment